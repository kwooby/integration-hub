{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Integration Hub",
            "type": "debugpy",
            "request": "launch",
            "module": "backend.app",
            "cwd": "${workspaceFolder}"
        }
    ]
}